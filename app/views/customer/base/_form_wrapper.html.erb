<%= simple_form_for @object, wrapper: 'horizontal_form' do |f| %>
  <%= render 'form', f: f %>

  <div class='actions'>
    <%= f.submit t('core.links.create'), class: 'btn btn-success' %>
  </div>
<% end %>
